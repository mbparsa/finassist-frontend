import React from 'react';

const Dashboard = ( () =>{
    return(
        <div>
        <h1>Dashboard</h1>
        <p>This is a placeholder page for displaying the user's dashboard.</p>
        </div>
    );
});

export default Dashboard;